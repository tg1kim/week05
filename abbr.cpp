// 복합 대입 연산자 프로그램

#include <stdio.h>

#pragma warning(disable: )

void main() 
{
	정수 x, y, z를 정의하고 10, 10, 33으로 초기화	
	x에 x + 1를 대입											// x = x + 1;
	y에 y * 2를 대입											// y = y * 2;
	z에 z % (10 + 20)를 대입									// z = z % (x + y );
	"x = %d,\ty = %d,\tz = %d\n"
}
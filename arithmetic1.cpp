#include <stdio.h>

#pragma warning(disable: )

void main() 
{
       정수 x, y, result를 정의
       "두개의 정수를 입력하시오: "
       x, y에 입력
       result = ?		                        // 덧셈 연산을 하여서 결과를 result에 대입
       "%d + %d = %d\n"
       result = ?;		                        // 뺄셈 연산
       "%d - %d = %d\n"
       result = ?		                        // 곱셈 연산
       "%d + %d = %d\n"
       result = ?		                        // 나눗셈 연산
       "%d / %d = %d\n"
       result = ?		                        // 나머지 연산
       "%d %% %d = %d\n"
}
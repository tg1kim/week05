#include <stdio.h>
#include <?.h>

#pragma warning(disable: )

void main() 
{
        2배정밀도 실수 a, b를 정의
        a에 0.3과 3의 곱 그리고 그 결과에 0.1과의 합을 대입
        b에 1을 대입
        "a == b의 결과: %d\n"
        "fabs(a - b) < 0.00001의 결과: %d\n"
}
#include <stdio.h>

기호상수 SEC_PER_MINUTE를 정의										// 1분은 60초

#pragma warning(disable: )

void main()
{

       정수 x, y를 정의하여 각각 0으로 초기화
       정수 result를 정의
       result = ?
       "2 > 3 || 6 > 7 = %d\n"
       result = ?
       "2 || 3 && 3 > 2 = %d\n"
       result, x, y에 1을 대입
       "result = %d\n"
       result = - ++x + y--;
       "- ++x + y-- = %d\n"
       "x = %d, y = %d\n"

}
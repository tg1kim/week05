#include <stdio.h>

#pragma warning(disable: )

void main() 
{
       정수 x, y를 정의
       "두개의 정수를 입력하시오: "
       x, y에 입력
       "%d && %d의 결과값: %d\n"
       "%d || %d의 결과값: %d\n"
       "     !%d의 결과값: %d\n"
}
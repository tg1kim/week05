#include <stdio.h>

#pragma warning(disable: )

void main() 
{
       정수 x, y를 정의
       "두개의 정수를 입력하시오: "
       x, y에 입력
       "x == y의 결과값: %d\n"
       x != y의 결과값: %d\n"
       " x > y의 결과값: %d\n"
       " x < y의 결과값: %d\n"
       "x >= y의 결과값: %d\n"
       "x <= y의 결과값: %d\n"
}
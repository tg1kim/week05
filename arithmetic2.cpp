#include <stdio.h>

#pragma warning(disable: )

void main() 
{
       2배정밀도 실수 x, y, result를 정의
       printf("두개의 실수를 입력하시오: ");
       x, y에 입력
       result = ?		                    // 덧셈 연산을 하여서 결과를 result에 대입
       "%f / %f = %f\n"
       result = ?		
       "%f - %f = %f\n"
       result = ?		
       "%f * %f = %f\n"
       result = ?		
       "%f / %f = %f\n"
}